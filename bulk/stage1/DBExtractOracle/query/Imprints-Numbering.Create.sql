CREATE TABLE DBE_Imprints(
    Id_Editor               INTEGER NOT NULL,
    IdentifierValue         VARCHAR2(256) NOT NULL,
    ImprintId               INTEGER NOT NULL,
    PRIMARY KEY(Id_Editor)
)